<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.jdr23bc.simplebackgrounds">

    <uses-feature android:name="android.software.live_wallpaper" android:required="true" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="ca.jdr23bc.simplebackgrounds.MainActivity" android:hardwareAccelerated="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="ca.jdr23bc.simplebackgrounds.SimpleWallpaperService"
            android:directBootAware="true"
            android:permission="android.permission.BIND_WALLPAPER"
            android:hardwareAccelerated="false">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
            </intent-filter>
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/simplewallpaper" />
        </service>
    </application>
</manifest>
